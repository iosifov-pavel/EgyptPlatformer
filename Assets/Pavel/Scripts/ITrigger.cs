using System.Collections;
using System.Collections.Generic;
using UnityEngine;

interface ITrigger
{
    //bool Trigger{get;set;}
    // Start is called before the first frame update
    void triggerAction(bool t);
}
